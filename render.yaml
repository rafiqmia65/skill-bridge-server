services:
  - type: web
    name: skill-bridge-server
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/server.js 
    envVars:
      - key: NODE_VERSION
        value: 24.11.0
