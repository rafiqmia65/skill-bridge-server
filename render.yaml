services:
  - type: web
    name: skill-bridge-server
    runtime: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_VERSION
        value: 25.2.3
